apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-flask-app-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-flask-app
  template:
    metadata:
      labels:
        app: my-flask-app
    spec:
      containers:
      - name: flask
        image: dev1s/my-flask-app:latest
        ports:
        - containerPort: 5000
        env:
        - name: MAIN_TEXT
          value: "Hello from hell"
